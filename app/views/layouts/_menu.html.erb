	<% menu = Page.where(:on_menu => true) %>
	<ul id="menu">
		<% menu.each do |m|%> 
			<li><%= link_to "<strong>#{m.title}</strong>".html_safe, page_path(m.uri) %></li>
		<% end %>
			<li><%= link_to "<strong>Наши объекты</strong>".html_safe, objects_path %></li>
	</ul>
